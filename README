Wesley Minner & Christie Mathews 
  703549234       104404412

CS 111 Operating Systems Principles
Professor Eggert
Winter 2016

***********************
Lab 1 - Simpleton Shell
***********************
Makefile functionality:
* make: compiles program 'simpsh'
* make dist: packages files into tarball and does some checks
* make check: performs some simple tests on simpsh
* make clean: removes all files created from 'make' and 'test.sh'


simpsh functionality:
* Working options
	--rdonly
	--wronly
	--rdwr
	--pipe
	--close
	--command
	--verbose
	--brief (cancel verbose on rest of commands)	
	--wait
	--test (used to serialize all commands for verification testing)
	--pause
	--abort
	--catch
	--default
	--ignore
* All oflags options supported
	--append
	--cloexec
	--creat
	--directory
	--dsync
	--excl
	--nofollow
	--nonblock
	--rsync
	--sync
	--trunc
* Verifies that the appropriate number of arguments are given
	--rdonly (1)
	--wronly (1)
	--command (>= 4)
* Can accept short arguments for commands (ex: "ls -l")

	
Lab 1a TODO:
* N/A


Lab 1b TODO:
* Pipe works when write end is closed before --wait, but --wait not outputting according to spec now (test 2)
* Need to save argv's of each command into a data structure with associated child pid, so it can be
  printed when childed is reaped (test 2)
* Depricate executecmd.c

  
Notes  
* test.sh runs simpsh with the "--test" option in order to avoid race conditions, 
  since there are multiple commands in tests and some depend on others.
* Option --test waits for each individual command to finish before starting a 
  new command (effectively serializing commands).  This is necessary for 
  verification testing only.
  
Known Issues
* None known

